name: bluesense_backend
description: Backend server for BlueSense Flutter app
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  dart_jsonwebtoken: ^2.13.0
  mongo_dart: ^0.10.8

dev_dependencies:
  lints: ^3.0.0
